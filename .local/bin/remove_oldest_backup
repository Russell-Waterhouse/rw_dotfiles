#!/usr/bin/expect -f

# This bash script deletes your oldest timeshift backup

set timeout 100

spawn sudo timeshift --delete

expect "Enter snapshot number (a=Abort, p=Previous, n=Next):"

send -- "0\r"

expect eof

